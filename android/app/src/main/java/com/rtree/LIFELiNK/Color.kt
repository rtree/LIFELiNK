package com.rtree.LIFELiNK

import androidx.compose.ui.graphics.Color

/** Palette extracted from the UI mocks in doc/uimock. */

// Navy — LIFELINK wordmark, headings, primary "Next" pill button.
val LifeLinkNavy = Color(0xFF0E2A55)
val LifeLinkNavyDark = Color(0xFF071B3A)
val LifeLinkNavyContainer = Color(0xFFDDE6F6)

// Emergency red — the SOS button and the incident-log accent bar.
val LifeLinkEmergencyRed = Color(0xFFE23B32)
val LifeLinkEmergencyRedDark = Color(0xFFB92B24)
// Pale halo ring drawn around the SOS button.
val LifeLinkEmergencyHalo = Color(0xFFFBE2E0)

// Accent blue — pagination dots, links, the audio-player play button.
val LifeLinkAccentBlue = Color(0xFF4557E8)
val LifeLinkAccentBlueContainer = Color(0xFFE6E9FD)

// Backgrounds and surfaces — near-white phone screen on a blue-tinted page.
val LifeLinkBackground = Color(0xFFF2F6FD)
val LifeLinkSurface = Color(0xFFFFFFFF)
val LifeLinkSurfaceVariant = Color(0xFFE8EEF8)
val LifeLinkOutline = Color(0xFFD5DEEC)

// Text.
val LifeLinkOnSurface = Color(0xFF15233C)
val LifeLinkOnSurfaceVariant = Color(0xFF66748C)

// Status green — the "Call ended" success accent.
val LifeLinkSuccessGreen = Color(0xFF2FA84F)
val LifeLinkSuccessGreenContainer = Color(0xFFE0F3E6)
val LifeLinkSuccessGreenDark = Color(0xFF14502A)
